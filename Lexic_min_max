#include <stdio.h>
#include <string.h>

int main()
{
    char x[1001], y[1001], z[1001];
    scanf("%s %s %s", x, y, z);

    if ((strcmp(x, y)) < 0 && (strcmp(x, z)) < 0)
    {
        printf("%s\n", x);
    }

    else if ((strcmp(y, x)) < 0 && (strcmp(y, z)) < 0)
    {
        printf("%s\n", y);
    }
    else
    {
        printf("%s\n", z);
    }

    if ((strcmp(x, y)) > 0 && (strcmp(x, z)) > 0)
    {
        printf("%s\n", x);
    }

    else if ((strcmp(y, x)) > 0 && (strcmp(y, z)) > 0)
    {
        printf("%s\n", y);
    }
    else
    {
        printf("%s\n", z);
    }

    return 0;
}
